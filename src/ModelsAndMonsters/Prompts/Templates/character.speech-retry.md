It looks like you tried to say something aloud, but you wrote it as plain text — so no one heard it.

To speak, call `say` and put only the words you speak in `message` — a short line, nothing else. If you meant to do something instead, call `take_action`; to wait, call `end_turn`.
